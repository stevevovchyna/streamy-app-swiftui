import SwiftUI

extension Color {
    static let shark = Color("shark")
    static let whisper = Color("whisper")
    static let codGrey = Color("codGrey")
    static let playbackOverlay = Color("playbackOverlay")
}
